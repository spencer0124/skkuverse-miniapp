# @skkuverse/tokens

SKKU Design System tokens for the web. `colors`, `typography`, `spacing` and `radius` are
copies of the skkuverse app's token files, hash-checked in CI, and `css.ts` converts them
to CSS units.

```ts
import { SdsColors, CssSpacing } from '@skkuverse/tokens';
```
