/**
 * React bindings for the miniapp SDK. `MapButton` needs `@skkuverse/ui`; the
 * hooks need only React.
 */
export { useIsInApp, useCapabilities, useCanOpenMap, useCanOpenMiniapp } from './hooks';
export { MapButton, type MapButtonProps } from './MapButton';
